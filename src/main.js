const matchCorner = require('./matchCorner')
const matchPattern = require('./matchPattern')
const findPatternInMove = require('./findPatternInMove')
const nameMove = require('./nameMove')

module.exports = {matchCorner, matchPattern, findPatternInMove, nameMove}
